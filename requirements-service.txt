# The deployed deterministic tier, and nothing else.
#
# DELIBERATELY NOT `requirements.txt`. That one is the BUILD's lock: it carries
# python-pptx, fonttools, tectonic's friends and grpcio-tools, because the Bazel
# rules need them to produce a full brand. The five RPCs this deployment serves
# emit SVG and JSON, so most of that would be weight with nothing behind it —
# and Vercel does not tree-shake Python, so everything reachable is shipped.
#
# shapely pulls numpy, which is most of the ~110 MB. Against a 500 MB ceiling
# that is comfortable, but it is worth knowing which line costs what.
shapely
svgwrite
# The wire, and the servicers' status codes. `grpcio` is here for
# `grpc.StatusCode` rather than for a server: nothing binds a port in this
# deployment.
grpcio
protobuf
googleapis-common-protos
