import { AuthenticatedStudio } from "@/app/_components/authenticated-studio";

/** A fresh brief, with no session attached yet. */
export default function NewRunPage() {
  return <AuthenticatedStudio />;
}
